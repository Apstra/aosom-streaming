

title: "System CPU"
template: prom-graph-lines-01.j2
datasource: prometheus
span: 6

targets:
  A:
    expr: sum(system_info_cpu_system{role=~"$role"}) by (device)
    legend: "{{ device }}"

# yaxes:
#   - format: bps
#     label: RX / TX
#   - format: short

# series_overrides:
#   - alias: "/rx/i"
#     transform: "negative-Y"

templatings_used:
  - role.yaml
